spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/smart_task_manager
    username: postgres
    password: Heptha@123456

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

    properties:
      hibernate:
        format_sql: true

server:
  port: 8081